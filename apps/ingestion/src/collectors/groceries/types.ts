export interface ScrapedProduct {
  brand: string;
  category: string;
  name: string;
  price: number;
  productId: string;
  size: string;
  source: string;
  store: string;
  unitPrice?: string;
}
