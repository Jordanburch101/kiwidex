[build]
dockerfilePath = "apps/ingestion/Dockerfile"
watchPatterns = ["apps/ingestion/**", "packages/db/**"]

[deploy]
numReplicas = 1
startCommand = "bun apps/ingestion/src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
